import { X } from "lucide-react";
const exclusions = ["Not a dating app", "No hookups", "No public profiles", "No likes, swipes, or popularity mechanics"];
const WhoThisIsNotFor = () => {
  return;
};
export default WhoThisIsNotFor;